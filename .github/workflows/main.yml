name: "MySQL CMD"
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: mysql:latest
    steps:
      - uses: actions/checkout@v2
    
      - name: Use files from Repo
        run: cat tests/a.txt
        
      - name: Run a multi-line script
        run: |
          pwd
          mysql --version
          mysql -h 104.155.173.97 -u root -p rajesh@123
        # cat tests/a.txt
